<h1>Register New User</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@user) do |f| %>
            <%= render 'fields', f: f %>

            <%= f.submit "Create user", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
